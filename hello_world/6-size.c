/**
 * main - sizeof
 *
 *
 * utiliza sizeof para mostrar el tamaño de los type
 *
 * Return: Always 0 (Success)
 */

#include <stdio.h>

int main(void)
{
	printf("Size of (int) = %lu" " bytes\n",
	sizeof(int)); 
	return (0);
}
