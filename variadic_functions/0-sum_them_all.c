#include "main.h"
/**
 * sum_them_all - returns the sum of all its parameters.
*/
int sum_them_all(const unsigned int n, ...)
{
int i;
if (n == 0)
{
return (0);
}

va_list args;

va_start(args, n);

for (i = 0; i < n; i++)
return (0);
}