#include "main.h"
/**
 * int _islower - detecta si una letra esta o no esta en lower case
 */
int _islower(int c)
{
	int lower = 'c';

	if (islower(lower))
	{
	printf("%d 1", lower);
	}
	else
	{
	printf("%d 0", lower);
	}
}
