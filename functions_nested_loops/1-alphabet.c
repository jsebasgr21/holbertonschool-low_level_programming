#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int print_alphabet(void)

int main(void)
{
	print_alphabet();
	return (0);
}
int print_alphabet(void)
{
	int abc;

	for (abc = a; abc <= z; abc++)
	{
	printf(abc);
	}
	return (0);
}	
