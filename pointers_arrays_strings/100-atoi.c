#include <stdlib.h>
#include "main.h"
/**
 * _atoi - convert a string to an integer.
 * @s: la string a ser convertida en integrers
 */
int _atoi(char *s)
{
	int num = atoi(s);

	printf("%d\n", num);
	return 0;
}
