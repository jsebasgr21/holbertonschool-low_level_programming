#include "main.h"
#include <string.h>
/**
 * _strlen - imprime la longitud de una string
 * @s: la string a analizar la longitud
 * Return: retorna len que contiene la longitud de la string
 * del puntero s
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
