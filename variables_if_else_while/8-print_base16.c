#include <stdio.h>
/**
 * main - all numbers base 16 in lowercase
 *
 *
 *
 *
 * Return: Always 0 (Succes)
*/
int main(void)
{
	int seis;

	for (seis = 0; seis <= 'f'; seis++)
	{
	putchar(seis);
	}
	putchar('\n');
	return (0);
}
