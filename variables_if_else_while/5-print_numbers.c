#include <stdio.h>
/**
 * main - imprime los numeros del 0 al 9
 * 
 * 
 * 
 * 
 * Return: Always 0 (Succes)
*/

int main(void)
{
	int numeros;

	for (numeros = 0; numeros <= 9; numeros++)
	{
	printf("%d", numeros);
	}
	printf("\n");
	return (0);
}
