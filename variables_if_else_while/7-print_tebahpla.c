#include <stdio.h>
/**
 * main - imprime los numeros del 0 al 9
 *
 *
 *
 *
 * Return: Always 0 (Succes)
*/
int main(void)
{
	int abc;

	for (abc = 'z'; abc >= 'a'; abc--)
	{
	putchar(abc);
	}
	putchar('\n');
	return (0);
}
